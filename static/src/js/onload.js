odoo.define('payment_sagepay.onload', function (require) {
"use strict";
var ajax = require('web.ajax');
    $(document).ready(function() {
        setTimeout('', 5000);
        $('#id_submit').click();
    });
});